import React from 'react'

import footerStyles from '../css/components/Footer.module.css'

const Footer = () => {

    return (
		<nav className={`${footerStyles.footer}`}>
		</nav>
	)
}

export default Footer